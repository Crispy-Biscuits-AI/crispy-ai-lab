OpenBrain Minimal Test File

OpenBrain is being developed incrementally inside a self-hosted AI lab.

This test file exists only to verify that the minimal ingest workflow
works correctly. The workflow should:

1.  Read this text file from disk
2.  Split the contents into one or more chunks
3.  Insert each chunk into the memories table

Expected metadata:

-   source: file_ingest
-   category: project_note
-   title prefix: 2026-04-14-openbrain-minimal-test

If everything is working correctly, the memories table should receive at
least one row with a title similar to:

2026-04-14-openbrain-minimal-test :: chunk 01

This file is intentionally short so that the first test run should
produce exactly one chunk and one inserted row.
